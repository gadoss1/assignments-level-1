// Forception Exercise //

// Vars and Funcs for Forception Exercise

var people = ["Jon", "Jacob", "Jingle", "Heimer", "Schmidt"];
var alphabet = "abcdefghijklmnopqrstuvwxyz";

function forception(people, alphabet){
    for (var i = people; i < people.length; i++){    
    } for (var j = alphabet; i < alphabet.length; j++){
        var newArray = i.concat(j);
        console.log(newArray);
    }
}