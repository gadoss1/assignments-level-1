// Node JS Calculator Project
    // Constant to perform redline-sync in terminal node for JS commands //
const readline = require("readline-sync/readline-sync");

var numbers = [15, 20, 25, 30];

// Consts and logic to add, subtract, divide, and multiply //
const operator = readlinesync.keyIn(add);

if(){

};

const operator = readlinesync.keyIn(subract);

if(){

};

const operator = readlinesync.keyIn(divide);

if(){

};

const operator = readlinesync.keyIn(multiply);

if(){

};



